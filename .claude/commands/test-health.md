Run the health check test.

```bash
npx vitest run test/health.test.ts
```

Tests: GET /health returns ok:true when DB is reachable.

Report results. If it fails, show failure details and suggest fixes.
