# 2026-02-07

- ETS Admin (Phase 2): **station mapped**.
  - Factory code: SOUTHERNIOT-DEMO
  - Station PK: st_mlb8cuk6_f1552eb8c45ccb6198a9
  - MAC: 10:97:BD:5A:23:04
  - Mapped station_id: L1-SW-01
  - Line: L1
  - Type: sewing
  - Screenshot message_id: 8a92a3ad-625f-4b7d-96c0-6ec6463de829

- Firmware/backend integration: send UID as **hex** as `rfid_uid` to backend.
  - Message_id: ec416521-ddce-4735-b5a8-10e843690df7

- Proceed with **firmware feature implementation** (RFID UID hex → backend).
  - Message_id: 086f8270-f339-4162-ae1d-fc5f1be6e40d

- Next step requested: proceed.
  - Message_id: 9470374f-2b03-420f-9b62-5ca136166f94

- Contract decision: use **default/recommended** approach now; adjust later if needed.
  - Message_id: 7c0fe42a-9b2c-44d9-9747-3fbfc809cfee

- Firmware environment: **PlatformIO (ESP-IDF)**; backend base URL: `APP_API_BASE = "http://192.168.1.198:3003"`.
  - Message_id: 1dfc7f55-6a06-4e52-8261-828252b1a940

- Checked code: backend event ingest endpoint is `POST /api/v1/events` (station-auth Bearer token).

- Hardware wiring (provided):
  - LCD ILI9341 SPI: CS=GPIO15, RST=GPIO4 (or -1), DC=GPIO2, MOSI=GPIO23, SCK=GPIO18, MISO=GPIO19 (opt)
  - Touch FT6336G I2C: SDA=GPIO21, SCL=GPIO22, RST=GPIO25 (opt)
  - RFID MFRC522 SPI (HSPI): SS=GPIO5, SCK=GPIO14, MOSI=GPIO13, MISO=GPIO12, RST=NC
  - Buzzer: GPIO33
  - RGB (active cathode): GPIO32/26/27

- Firmware change: implemented MFRC522 UID read + post COMPLETE event to backend with `bundle.rfid_uid` (hex).

- Issue: station shows ONLINE but still **unmapped** in admin UI; RFID reader not working; LCD blank; need RGB+buzzer confirmation.
  - Message_id: 1db39078-8658-4201-ba58-c61d36bd8f1f

- After upload+monitor: device shows repeated `heartbeat ok` logs; captured boot log shows MFRC522 VersionReg=0x92 and init OK; WiFi connected and SNTP started.
  - Message_id: 02732897-d526-402e-859c-2df6d4c4bd6e

- Implemented LCD support (ILI9341 raw SPI, VSPI/SPI3) with status screen; firmware builds.

- Fixes / additions in `projects/rmg-rfid-station-fw`:
  - MFRC522 (HSPI) driver: VersionReg check, REQA/WUPA retries, comms auto-reset, de-dup, event POST to backend `POST /api/v1/events` with `bundle.rfid_uid`.
  - Station mapping: resolved station_id conflict (SIM station renamed; real station mapped to L1-SW-01).
  - RGB (active cathode) + buzzer feedback on reads/posts.
  - LCD: orientation fixed (MADCTL tweak), reduced flicker by avoiding full-screen clears.
  - Touch: FT6336 (I2C addr 0x38) working; calibrated mapping to UI 320x240; on-screen buttons (REFRESH / BUZ toggle) + touch crosshair.
  - Buzzer: switched to LEDC PWM tone + startup frequency sweep to find resonance; increased duty for louder output.

- Latest touch calibration (from corner taps):
  - top-left raw(0,293) -> ui(0,0)
  - top-right raw(12,3) -> ui(319,0)
  - bottom-left raw(239,292) -> ui(0,239)

- Next TODOs:
  - Pick loudest buzzer sweep frequency and set as default.
  - When RFID cards available: verify UID reading + backend event flow end-to-end.

- Session label: **careflow-new-crm**

- SCOMM CRM frontend theme tweaks (repo: `/Users/robotics/southerniot-scomm-crm-stack/scomm-crm/frontend`)
  - Identified stack from running page: Next.js App Router + Turbopack + Tailwind v4 + next/font; devtools include TanStack Query Devtools + Next Dev Tools.
  - Implemented centered timestamp line above sign-in form (`components/auth/Login.Client.tsx`).
  - Added Southerniot logo asset `public/southerniot-logo.svg` and swapped the top-left label to the logo.
  - Noted: app uses `basePath` default `/crm` (see `next.config.ts`), so assets are served under `/crm/*`.
  - Created docs:
    - `docs/DEVELOPMENT.md` (basePath + assets + lock troubleshooting)
    - `docs/DEPLOYMENT.md` (env/basePath deployment notes)
    - `docs/STYLE.md` (login theme conventions)

- ETS/RFID: created CEO-ready docs + diagrams; exported Mermaid to PNG/SVG and PDF bundle; created private repo `rnd-southerniot/rmg-rfid-ets` and pushed.
- Updated `projects/rmg-rfid-ets/NEXT_STEPS.md` with a "work later" pilot plan (runbook → firmware E2E → admin tooling → reliability → acceptance checklist).
